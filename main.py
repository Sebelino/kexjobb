#!/usr/bin/env python

while True:
    current_brightness = get_brightness.py
     
    if höja:
        current_brightness += 10
    if sänka:
        current_brightness -= 10
    illuminator = current_brightness.py

